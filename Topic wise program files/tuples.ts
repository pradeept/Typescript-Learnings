//These are the stricter version of arrays

// Here we define type of every element in the array

//id, name, fulltime
let employee: [number, string, boolean];

employee = [1, "Tom", true];
// employee[0] = ["John"];  // will throw an error saying can't assign string to number type

